import {
    getTodos,
    getTages,
    updateTodo,
    updateStep,
    addNewTodo,
    addNewTag,
    createEmptyTodo,
    createEmptyStep,
    createEmptyTag,
} from './api'

// Types
import type { Todo, Step, Tag } from './api'


export {
    getTodos,
    getTages,
    updateTodo,
    updateStep,
    addNewTodo,
    addNewTag,
    createEmptyTodo,
    createEmptyStep,
    createEmptyTag,
}

export type {
    Todo,
    Step,
    Tag,
}
